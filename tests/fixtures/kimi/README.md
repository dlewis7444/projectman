# Kimi Code fixtures

Sanitized schema samples for unit tests (no secrets).

- `config.toml` — model aliases + default_model (api_key strings empty)
- `session_index.jsonl` — three project sessions + one other-project row
- `state_*.json` — state.json samples with titles and ISO timestamps
